export default class ModelSelectState
{
    NodeID = $state(0);
    ModelID = $state("");
    ProviderID = $state("");
    ProviderTab = $state("");

    FilterName = $state("");
    FilterFree = $state(false);
}